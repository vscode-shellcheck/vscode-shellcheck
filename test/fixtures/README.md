# ShellCheck diagnostic corpus

`shellcheck-messages.golden.json` contains ShellCheck diagnostic messages and
their expected Markdown output. Run `npm run test:unit` to verify this corpus
without starting VS Code.
